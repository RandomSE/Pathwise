[pytest]
testpaths = tests
timeout = 30
timeout_method = thread
markers =
    needs_map_bake: start_round should rasterize map tiles (real Arcade window)
filterwarnings =
    ignore:.*ctypes._pointer_type_cache.*:DeprecationWarning
