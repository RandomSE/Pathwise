# Synthetic CI / test fixture. Not a real Turso database. Never a JWT.
# Used only so pack/COLLECT can embed an obfuscated blob without operator keys.

TURSO_DATABASE_URL=libsql://ci-placeholder.example.invalid
TURSO_AUTH_TOKEN=ci-not-a-real-token

PATHWISE_SMTP_HOST=
PATHWISE_SMTP_PORT=587
PATHWISE_SMTP_USER=
PATHWISE_SMTP_PASSWORD=
PATHWISE_SMTP_FROM=

PATHWISE_REQUIRE_BILLING=
PATHWISE_SEED=
